// ============================================
// ROUTES INDEX - Export all route modules
// ============================================

const productRoutes = require('./productRoutes');

module.exports = {
    productRoutes
};
